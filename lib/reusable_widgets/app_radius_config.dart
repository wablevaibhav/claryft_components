import 'package:flutter/material.dart';

class CjRadiusConfig {
  CjRadiusConfig._();
  static const BorderRadius radiusNone = BorderRadius.all(Radius.circular(0));
  static const BorderRadius radiusXxs = BorderRadius.all(Radius.circular(2));
  static const BorderRadius radiusXs = BorderRadius.all(Radius.circular(4));
  static const BorderRadius radiusSm = BorderRadius.all(Radius.circular(6));
  static const BorderRadius radiusMd = BorderRadius.all(Radius.circular(8));
  static const BorderRadius radiusLg = BorderRadius.all(Radius.circular(10));
  static const BorderRadius radiusXl = BorderRadius.all(Radius.circular(12));
  static const BorderRadius radius2xl = BorderRadius.all(Radius.circular(16));
  static const BorderRadius radius3xl = BorderRadius.all(Radius.circular(20));
  static const BorderRadius radius4xl = BorderRadius.all(Radius.circular(24));
  static const BorderRadius radiusFull = BorderRadius.all(
    Radius.circular(9999),
  );
}
